// Placeholder Firebase initialization
// Project saat ini menggunakan static data (src/data/*) dan belum terhubung ke Firebase/Firestore.
// File ini disiapkan sesuai struktur target src/lib/firebase.ts agar siap saat integrasi Firebase ditambahkan.
// Contoh inisialisasi (uncomment & isi config saat ready):
// import { initializeApp } from "firebase/app";
// import { getFirestore } from "firebase/firestore";
// const firebaseConfig = { apiKey: process.env.NEXT_PUBLIC_FIREBASE_API_KEY, ... };
// export const app = initializeApp(firebaseConfig);
// export const db = getFirestore(app);
export {};
